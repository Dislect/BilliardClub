@model List<BilliardClub.Models.CartItem>

@if (Model.Count != 0)
{
    <h1 style="color: aquamarine"> Выбранные столики: </h1>

    @foreach (var item in Model)
    {
        <h2 style="color: antiquewhite"> Стол № @item.PoolTable.name . Тип стола - @item.PoolTable.typeTable.name </h2>
    }
}

